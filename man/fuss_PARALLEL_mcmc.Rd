% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MultiModalR.R
\name{fuss_PARALLEL_mcmc}
\alias{fuss_PARALLEL_mcmc}
\title{Parallel Bayesian mixture modeling using Markov Chain Monte Carlo (MCMC)}
\usage{
fuss_PARALLEL_mcmc(
  data,
  varCLASS,
  varY,
  varID,
  method = "sj-dpi",
  within = 1,
  maxNGROUP = 5,
  out_dir = NULL,
  n_workers = 3,
  n_iter = NULL,
  burnin = NULL,
  proposal_sd = 0.15,
  sj_adjust = 0.5,
  mcmc_method = "metropolis",
  dirichlet_alpha = 2
)
}
\arguments{
\item{data}{Input data frame}

\item{varCLASS}{Character, category variable name (required)}

\item{varY}{Character, value variable name (required)}

\item{varID}{Character, ID variable name (required)}

\item{method}{Density estimator method ("sj-dpi", "bcv", "ucv", "nrd") (default: "sj-dpi")}

\item{within}{Range parameter for grouping modes (default: 1.0)}

\item{maxNGROUP}{Maximum number of groups (default: 5)}

\item{out_dir}{Output directory for CSV files (if NULL, returns combined data frame)}

\item{n_workers}{Number of parallel workers (default: 3)}

\item{n_iter}{Number of MCMC iterations (default: 1000 for metropolis, 3000 for dirichlet)}

\item{burnin}{Burn-in period (default: 500 for metropolis, 1000 for dirichlet)}

\item{proposal_sd}{Proposal standard deviation for component means (default: 0.15)}

\item{sj_adjust}{Adjustment factor for bandwidth methods (default: 0.5, smaller -> more modes, higher -> fewer modes)}

\item{mcmc_method}{"metropolis" or "dirichlet"(default: "metropolis")}

\item{dirichlet_alpha}{Dirichlet concentration parameter (default: 2.0)}
}
\value{
Data frame (if out_dir is NULL) or writes CSV files
}
\description{
Performs multimodal probability assignment using either:
1. Metropolis-Hastings-within-partial-Gibbs 
2. Dirichlet-Multinomial
}
